
type TrackItem @model @key(
  name: "byCreatedDate"
  fields: ["userId", "createdAt"]
  queryField: "itemsByCreatedAt"
) {
  id: ID!
  userId: String!
  pin: String!
  description: String
  createdAt: AWSDateTime
}
